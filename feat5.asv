close all;

cd data\feat4\

C50_a = [];
C50_b = [];
dist = [0.5, 1, 2, 3];
room = ["a", "b"];

for ii = room
    for jj = dist
        
        filename = sprintf("imp_resp_%s_%s.mat", num2str(jj), ii);
        imp_resp = load(filename).y;
        
        if ii == "a"
            
        else 
            
        end
    end
end

cd ../..